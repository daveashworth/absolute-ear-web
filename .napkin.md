# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|-----------------|--------------------|
| 2026-05-02 | self | Parallelized `git remote add` and `git push`, so the push ran before `origin` existed | Run dependent git commands sequentially; only parallelize independent reads/checks |

## User Preferences
- When providing screenshot assets for the landing page, use the supplied iPhone wireframe images directly instead of recreating the screen in code.

## Patterns That Work
- Source screenshots live under `/Users/daveashworth/data/wireview/iPhone`; `01-home.png` matches the landing-page phone screenshot.
- Keep the landing page hero wired through `src/components/PhoneMockup.tsx`; swapping the image there avoids touching the page layout.
- For curriculum marketing copy, use `../pianoear/tasks/prd-chord-modules.md` plus `../pianoear/docs/features/chords-progressions-copy-review.md` as the product source of truth.
- Store-launch messaging and download links on the marketing site are centralized in `src/pages/index.astro`; updating that one file covers the nav, hero CTA, and download section.

## Patterns That Don't Work
- Assuming an image already exists in `public/` is unreliable; verify the actual asset before wiring UI changes.

## Domain Notes
- Landing page phone imagery is being replaced with real app screenshots rather than SVG or simulated UI.
